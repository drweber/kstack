id: audit-security-legacy-sa-token
skill: audit-security
description: >
  Verifies that /audit-security flags a long-lived legacy
  kubernetes.io/service-account-token Secret — the pre-v1.24 mechanism
  that the projected-token system replaces.
namespace: eval-audit-security-legacy-sa-token
runs: 3
pass_threshold: 2
wait_seconds: 30
claude_flags:
  model: ""
  allowed_tools: "Bash"
