id: audit-network-ingress-missing-backend
skill: audit-network
description: >
  Verifies that /audit-network flags an Ingress whose backend points at
  a Service that does not exist — the API-only Workflow 3 check.
namespace: eval-audit-network-ingress-missing-backend
runs: 3
pass_threshold: 2
wait_seconds: 30
claude_flags:
  model: ""
  allowed_tools: "Bash"
