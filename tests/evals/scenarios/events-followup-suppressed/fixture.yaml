apiVersion: v1
kind: Pod
metadata:
  name: quiet-sleeper
spec:
  containers:
    - name: quiet-sleeper
      image: busybox:latest
      command: ["sleep", "3600"]
      resources:
        requests:
          cpu: 10m
          memory: 16Mi
  restartPolicy: Never
