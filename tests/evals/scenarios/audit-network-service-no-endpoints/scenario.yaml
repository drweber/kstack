id: audit-network-service-no-endpoints
skill: audit-network
description: >
  Verifies that /audit-network flags a Service whose selector matches no
  pods (zero ready endpoints) — the API-only Workflow 2 check.
namespace: eval-audit-network-service-no-endpoints
runs: 3
pass_threshold: 2
wait_seconds: 30
claude_flags:
  model: ""
  allowed_tools: "Bash"
