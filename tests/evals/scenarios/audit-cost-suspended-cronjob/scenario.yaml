id: audit-cost-suspended-cronjob
skill: audit-cost
description: >
  Verifies that /audit-cost flags a CronJob with spec.suspend=true — the
  API-only Workflow 2 (idle workloads) check.
namespace: eval-audit-cost-suspended-cronjob
runs: 3
pass_threshold: 2
wait_seconds: 30
claude_flags:
  model: ""
  allowed_tools: "Bash"
