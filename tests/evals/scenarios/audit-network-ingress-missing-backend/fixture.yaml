apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ghost-ingress
spec:
  rules:
    - host: ghost.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nonexistent-svc
                port:
                  number: 80
