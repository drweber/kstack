apiVersion: v1
kind: Pod
metadata:
  name: bad-puller
spec:
  containers:
    - name: bad-puller
      image: busybox:this-tag-does-not-exist
      resources:
        requests:
          cpu: 10m
          memory: 16Mi
  restartPolicy: Never
